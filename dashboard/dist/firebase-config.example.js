// Copy this file to firebase-config.js (gitignored) and fill in your keys.
// This is only needed for local development — Firebase Hosting auto-configures
// the SDK via the /__/firebase/ reserved endpoints.
const FIREBASE_CONFIG = {
  apiKey: "your-api-key",
  projectId: "your-project-id",
};
